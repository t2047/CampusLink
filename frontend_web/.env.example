# API 基址（后端根地址，可选）：
# - 留空：同源相对路径，由 vite proxy 把 /api 转发到 http://localhost:8080
# - 部署：指向后端根（如 https://api.example.com），需后端 CORS 放行前端 origin
# 注意：两个 API 通道（axios client.ts / fetch services/api.ts）统一读此变量
VITE_API_BASE=
